# Core fine-tuning stack
transformers==4.44.2
trl==0.10.1
peft==0.12.0
accelerate==0.33.0
datasets==2.20.0
safetensors==0.4.4
sentencepiece==0.2.0
rich==13.7.1

# Use a torch build matching your CUDA/CPU environment
# Example (CUDA): pip install torch --index-url https://download.pytorch.org/whl/cu121
# Example (CPU):  pip install torch
# Optional (Linux CUDA only): pip install bitsandbytes==0.43.3
